apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jitsi-helm-ingress-room1
  namespace: jitsi
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    cert-manager.io/acme-challenge-type: http01
    kubernetes.io/ingress.class: "nginx"
spec:
  ingressClassName: nginx
  # This section is only required if TLS is to be enabled for the Ingress
  tls:
    - hosts:
        - room1.istudiomeet.com
      secretName: tls-jitsi-room1
  rules:
    - host: room1.istudiomeet.com
      http:
        paths:
          - pathType: Prefix
            backend:
              service:
                name: istudiomeet-helm-jitsi-meet-web
                port:
                  number: 80
            path: /
